spring:
  application:
    name: qi-ai-agent
  ai:
    deepseek:
      api-key: ${DEEPSEEK_API_KEY:your-key}
      chat:
        options:
          model: deepseek-chat
          temperature: 0.8

server:
  port: 8080

logging:
  level:
    org.springframework.ai: DEBUG